# prython-ssh-brute-force-detector
Python-based SSH brute-force detection tool that analyzes  auth.log, identifies suspicious IP activity, and correlates failed and successful logins to detect potential system compromise.
